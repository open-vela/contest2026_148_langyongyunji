#
# VelaGuard configuration holder.  The official SiFli BSP is selected from
# configs/nsh/defconfig, so this template symbol is not enabled for the
# Huangshan Pi build.
#

if ARCH_BOARD_CONTEST2026_000_BOARD

config ARCH_BOARD_CONTEST2026_000_BOARD_TAG
	string "Board tag"
	default "contest2026_000_board"

endif
