diff --git a/chips/sf32lb52/sf32lb52_bt_adapter.c b/chips/sf32lb52/sf32lb52_bt_adapter.c
--- a/chips/sf32lb52/sf32lb52_bt_adapter.c
+++ b/chips/sf32lb52/sf32lb52_bt_adapter.c
@@ -934,4 +934,9 @@ int sf32lb52_host_send_packet(const uint8_t *data, uint16_t len)
       return -ENODEV;
     }
 
+  if (data[0] == 0x02)
+    {
+      syslog(LOG_INFO, "sf32lb52 bt tx acl stage=before-lock\n");
+    }
+
   ret = nxmutex_lock(&g_sf32lb52_bt_tx_lock);
@@ -949,4 +954,13 @@ int sf32lb52_host_send_packet(const uint8_t *data, uint16_t len)
       return ret;
     }
 
+  if (data[0] == 0x02)
+    {
+      syslog(LOG_INFO,
+             "sf32lb52 bt tx acl stage=locked buf=%d rd=%08lx wr=%08lx\n",
+             tx_ring->buffer_size,
+             (unsigned long)tx_ring->read_idx_mirror,
+             (unsigned long)tx_ring->write_idx_mirror);
+    }
+
   start_time = HAL_GetTick();
@@ -964,4 +977,13 @@ int sf32lb52_host_send_packet(const uint8_t *data, uint16_t len)
       goto unlock;
     }
 
+  if (data[0] == 0x02)
+    {
+      syslog(LOG_INFO,
+             "sf32lb52 bt tx acl stage=idle buf=%d rd=%08lx wr=%08lx\n",
+             tx_ring->buffer_size,
+             (unsigned long)tx_ring->read_idx_mirror,
+             (unsigned long)tx_ring->write_idx_mirror);
+    }
+
   for (;;)
@@ -979,4 +1000,14 @@ int sf32lb52_host_send_packet(const uint8_t *data, uint16_t len)
       written = sf32lb52_bt_ring_write(tx_ring, data, len);
       leave_critical_section(flags);
 
+      if (data[0] == 0x02)
+        {
+          syslog(LOG_INFO,
+                 "sf32lb52 bt tx acl stage=ring-write written=%lu buf=%d rd=%08lx wr=%08lx\n",
+                 (unsigned long)written,
+                 tx_ring->buffer_size,
+                 (unsigned long)tx_ring->read_idx_mirror,
+                 (unsigned long)tx_ring->write_idx_mirror);
+        }
+
       if (written == len)
@@ -1020,3 +1020,8 @@ int sf32lb52_host_send_packet(const uint8_t *data, uint16_t len)
   wr_ptr = tx_ring->write_idx_mirror;
   sf32lb52_bt_trigger_tx();
 
+  if (data[0] == 0x02)
+    {
+      syslog(LOG_INFO, "sf32lb52 bt tx acl stage=triggered\n");
+    }
+
